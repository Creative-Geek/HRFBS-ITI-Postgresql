# HRFBS - APP

This app was done in a matter of minutes, don't expect anything polished, it's acting as a glorified DB UI.

To get started run `uv sync` then `uv run uvicorn main:app --port 8000`

Make sure to install `uv` (the python package manager) first.
